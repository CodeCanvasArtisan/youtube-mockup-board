import styles from "./toolbar.module.css";
import {useState, useRef, useEffect} from "react";

export function TopToolbar({videoName}) {
    return (
        <section className={styles.container}>
            asdf
        </section>
    )
}